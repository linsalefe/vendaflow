# ─── DATABASE ───────────────────────────���──────────────────────────────
DATABASE_URL=postgresql+asyncpg://vendaflow_user:CHANGE_ME@localhost:5432/vendaflow

# ─── FRONTEND ──────────────────────���───────────────────────────────────
FRONTEND_URL=http://localhost:3000

# ─── OPENAI ──────────────────��────────────────────────────���────────────
OPENAI_API_KEY=sk-...

# ─── EVOLUTION API (WhatsApp) ────────────────────────��─────────────────
EVOLUTION_API_URL=https://evo.seudominio.com
EVOLUTION_API_KEY=sua-api-key

# ─── ELEVENLABS (opcional, feature flag por tenant) ──────────────���────
# ELEVENLABS_API_KEY=sk_...

# ─── STRIPE / GATEWAYS (configurados por tenant no banco) ─────────────
# As chaves específicas de gateway ficam no banco (tabela tenants)
# Aqui podemos ter um default global de fallback
# STRIPE_SECRET_KEY=sk_live_...

# ─── JWT ───────────────────────────────────────────────────────────────
JWT_SECRET_KEY=CHANGE_ME_use_openssl_rand_hex_32
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24
